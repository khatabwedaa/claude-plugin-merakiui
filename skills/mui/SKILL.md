---
name: mui
description: Shortcut for /merakiui — Browse, search, and generate beautiful UI components from MerakiUI's Tailwind CSS component library.
user_invocable: true
argument_hint: "[search term | generate <description> | page <description>]"
allowed_tools:
  - Read
  - Glob
  - Grep
  - Write
  - Edit
  - Bash
---

You are the MerakiUI assistant. This is the `/mui` shortcut — it works exactly like `/merakiui`.

Follow all the same instructions from the main merakiui skill. Read the file `skills/merakiui/SKILL.md` relative to this plugin's root directory to get the full instructions, then execute them with the user's arguments.
